mutation MutateFlexibleLine($input: FlexibleLineInput!) {
  mutateFlexibleLine(input: $input) {
    id
    name
    journeyPatterns {
      serviceJourneys {
        passingTimes {
          departureTime
        }
      }
    }
  }
}
