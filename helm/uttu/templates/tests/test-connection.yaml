apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uttu.fullname" . }}-test-connection"
  labels:
    {{- include "uttu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uttu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
